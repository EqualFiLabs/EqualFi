# Echidna config for EqualIndexFacetV3

testMode: "assertion"
seqLen: 75
testLimit: 50000
shrinkLimit: 5000
coverage: true
corpusDir: "echidna-corpus-index"
coverageFormats: ["txt", "html"]
seed: 42
format: "text"

allowFFI: false
filterFunctions: ["setup"]
filterBlacklist: true

cryticArgs: ["--compile-force-framework", "foundry"]
