# Echidna config for MamCurve Full Fuzzing

testMode: "assertion"
seqLen: 75
testLimit: 50000
shrinkLimit: 5000
coverage: true
corpusDir: "echidna-corpus-mam-full"
coverageFormats: ["txt", "html"]
seed: 42
format: "text"

allowFFI: false
filterFunctions: ["setup"]
filterBlacklist: true

cryticArgs: ["--compile-force-framework", "foundry", "--compile-ignore-filenames", "src/erc6900/PositionMSCA.sol,src/erc6900/PositionMSCAImpl.sol"]
